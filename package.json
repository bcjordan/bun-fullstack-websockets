{
  "name": "bun-full-stack",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "bun run src/server.ts",
    "build": "bun build src/client/index.ts --outdir public --outfile app.js",
    "start": "bun run src/server.ts"
  },
  "devDependencies": {
    "bun-types": "latest"
  }
}